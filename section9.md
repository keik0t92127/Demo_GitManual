# 9. マージ

別のブランチの変更を現在のブランチに統合します。

## 手順

1. 統合先のブランチに切り替える
   ```
   git switch main
   ```
2. 対象ブランチをマージする
   ```
   git merge ブランチ名
   ```
3. コンフリクトが発生した場合、該当ファイルを手動で編集する
4. 編集後、ステージしてコミットする
   ```
   git add .
   git commit -m "コンフリクト解消"
   ```

> コンフリクト発生時は、`<<<<<<<` と `>>>>>>>` のマーカーを目印に修正してください。
