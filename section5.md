# 5. 状態の確認

作業ツリーやステージングの状態を確認します。

## 手順

1. 現在の状態を確認する
   ```
   git status
   ```
2. 短縮形式で確認する
   ```
   git status -s
   ```

## 表示の見方

| 記号 | 意味 |
|------|------|
| `M`  | 変更あり |
| `A`  | 新規追加（ステージ済み） |
| `D`  | 削除 |
| `??` | 未追跡ファイル |

> `git status` はこまめに実行して、現在の作業状態を把握する習慣をつけましょう。
