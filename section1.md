# ⚙️ 1. Git の初期設定

> **概要:** Git を使い始める前に、ユーザー名とメールアドレスを設定します。  
> この設定はすべてのコミットに記録されます。

---

## 📋 手順

### 1. ユーザー名を設定する

```bash
git config --global user.name "あなたの名前"
```

### 2. メールアドレスを設定する

```bash
git config --global user.email "you@example.com"
```

### 3. 設定内容を確認する

```bash
git config --list
```

---

## 🗒️ 設定ファイルの場所

| スコープ | オプション | 設定ファイルの場所 |
|---------|-----------|-----------------|
| システム全体 | `--system` | `/etc/gitconfig` |
| ユーザー全体 | `--global` | `~/.gitconfig` |
| リポジトリのみ | （省略） | `.git/config` |

---

> 💡 **ヒント:**  `--global` を付けると、すべてのリポジトリに共通の設定になります。  
> リポジトリごとに変えたい場合は `--global` を省略してください。

---

[← マニュアル トップへ](gitmanual.md) ｜ [次のセクションへ → リポジトリの作成](section2.md)
