# 💾 4. コミットの作成

> **概要:** ステージングした変更をリポジトリに記録します。

---

## 📋 手順

### 1. メッセージを付けてコミットする

```bash
git commit -m "コミット メッセージ"
```

### 2. ステージングとコミットを同時に行う（追跡済みファイルのみ）

```bash
git commit -am "コミット メッセージ"
```

### 3. 直前のコミット メッセージを修正する

```bash
git commit --amend -m "新しいメッセージ"
```

---

## ✍️ 良いコミット メッセージの書き方

| 種類 | 例 |
|------|-----|
| ✅ 良い例 | `feat: ユーザー認証機能を追加` |
| ✅ 良い例 | `fix: ログイン時のクラッシュを修正` |
| ❌ 悪い例 | `修正` |
| ❌ 悪い例 | `update` |

> 何を・なぜ変更したかが一目で分かるメッセージを心がけましょう。

---

> 💡 **ヒント:** `--amend` は直前のコミットを上書きします。  
> すでにリモートにプッシュ済みのコミットを `--amend` すると、履歴が変わって問題が起きることがあります。注意して使いましょう。

---

[← 前のセクション: ファイルのステージング](section3.md) ｜ [← マニュアル トップへ](gitmanual.md) ｜ [次のセクションへ → 状態の確認](section5.md)
