# 7. 差分の確認

ファイルの変更内容（差分）を確認します。

## 手順

1. 作業ツリーとステージの差分を表示する
   ```
   git diff
   ```
2. ステージと最新コミットの差分を表示する
   ```
   git diff --staged
   ```
3. 特定ファイルの差分を表示する
   ```
   git diff ファイル名
   ```
4. コミット間の差分を表示する
   ```
   git diff コミットID1 コミットID2
   ```

> `git diff` は未ステージの変更、`git diff --staged` はステージ済みの変更を確認できます。
