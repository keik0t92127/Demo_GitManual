# ↩️ 10. 変更の取り消し

> **概要:** 誤った変更やコミットを取り消します。

---

## 📋 手順

### 1. 作業ツリーの変更を元に戻す

```bash
git restore ファイル名
```

### 2. ステージングを取り消す（変更は残る）

```bash
git restore --staged ファイル名
```

### 3. 直前のコミットを取り消す（変更は残る）

```bash
git reset --soft HEAD~1
```

### 4. 直前のコミットを完全に取り消す（変更も消える）

```bash
git reset --hard HEAD~1
```

---

## ⚠️ コマンドの違いと影響範囲

| コマンド | 作業ツリー | ステージング | コミット履歴 | 安全度 |
|---------|:--------:|:-----------:|:-----------:|:------:|
| `git restore ファイル名` | 変更が消える | 変わらない | 変わらない | ⚠️ 要注意 |
| `git restore --staged ファイル名` | 変わらない | 取り消し | 変わらない | ✅ 安全 |
| `git reset --soft HEAD~1` | 変わらない | 変わらない | 1つ戻る | ✅ 安全 |
| `git reset --mixed HEAD~1` | 変わらない | 取り消し | 1つ戻る | ✅ 安全 |
| `git reset --hard HEAD~1` | **消える** | 取り消し | 1つ戻る | 🚨 危険 |

---

> 🚨 **警告:** `--hard` は作業ツリーの変更が**完全に消えます**。慎重に使用してください。  
> 誤って `--hard` を実行した場合でも、`git reflog` で直前の状態に戻せる場合があります。

---

> 💡 **ヒント:** 迷ったときは `--soft` を選ぶと安全です。  
> コミット履歴だけを取り消して、変更内容は保持されます。

---

[← 前のセクション: マージ](section9.md) ｜ [← マニュアル トップへ](gitmanual.md)
