# 10. 変更の取り消し

誤った変更やコミットを取り消します。

## 手順

1. 作業ツリーの変更を元に戻す
   ```
   git restore ファイル名
   ```
2. ステージングを取り消す（変更は残る）
   ```
   git restore --staged ファイル名
   ```
3. 直前のコミットを取り消す（変更は残る）
   ```
   git reset --soft HEAD~1
   ```
4. 直前のコミットを完全に取り消す（変更も消える）
   ```
   git reset --hard HEAD~1
   ```

> `--hard` は変更が完全に消えるため、慎重に使用してください。
