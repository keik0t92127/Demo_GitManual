# 📁 2. リポジトリの作成

> **概要:** プロジェクトのフォルダを Git リポジトリとして初期化します。

---

## 📋 手順

### 1. プロジェクト用のフォルダを作成する

```bash
mkdir myproject
```

### 2. フォルダに移動する

```bash
cd myproject
```

### 3. Git リポジトリを初期化する

```bash
git init
```

### 4. `.git` フォルダが作成されたことを確認する

```bash
ls -a
```

---

## 📂 `git init` 後のフォルダ構成

```
myproject/
├── .git/          ← バージョン管理情報が格納される隠しフォルダ
│   ├── HEAD
│   ├── config
│   ├── objects/
│   └── refs/
└── （あなたのファイル）
```

---

> 💡 **ヒント:** `git init` を実行すると、隠しフォルダ `.git` が作成され、バージョン管理が開始されます。  
> `.git` フォルダを削除すると、バージョン管理の履歴もすべて失われるので注意してください。

---

[← 前のセクション: Git の初期設定](section1.md) ｜ [← マニュアル トップへ](gitmanual.md) ｜ [次のセクションへ → ファイルのステージング](section3.md)
