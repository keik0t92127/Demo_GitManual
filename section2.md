# 2. リポジトリの作成

プロジェクトのフォルダを Git リポジトリとして初期化します。

## 手順

1. プロジェクト用のフォルダを作成する
   ```
   mkdir myproject
   ```
2. フォルダに移動する
   ```
   cd myproject
   ```
3. Git リポジトリを初期化する
   ```
   git init
   ```
4. `.git` フォルダが作成されたことを確認する
   ```
   ls -a
   ```

> `git init` を実行すると、隠しフォルダ `.git` が作成され、バージョン管理が開始されます。
