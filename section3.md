# 3. ファイルのステージング

変更したファイルをコミット対象として登録（ステージング）します。

## 手順

1. 特定のファイルをステージングする
   ```
   git add ファイル名
   ```
2. 複数のファイルをまとめてステージングする
   ```
   git add ファイル1 ファイル2
   ```
3. すべての変更をステージングする
   ```
   git add .
   ```
4. ステージング状態を確認する
   ```
   git status
   ```

> ステージングは「次のコミットに含める変更を選ぶ」操作です。`git add .` はカレント ディレクトリ以下のすべての変更を対象にします。
