# 6. 履歴の確認

コミットの履歴を表示して、過去の変更内容を確認します。

## 手順

1. コミット履歴を表示する
   ```
   git log
   ```
2. 各コミットを1行で表示する
   ```
   git log --oneline
   ```
3. グラフ形式でブランチの流れを表示する
   ```
   git log --oneline --graph
   ```
4. 特定ファイルの履歴を表示する
   ```
   git log ファイル名
   ```

> `q` キーでログ表示を終了できます。`--oneline` を付けると一覧性が向上します。
