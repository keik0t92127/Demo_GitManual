# 📜 6. 履歴の確認

> **概要:** コミットの履歴を表示して、過去の変更内容を確認します。

---

## 📋 手順

### 1. コミット履歴を表示する

```bash
git log
```

### 2. 各コミットを 1 行で表示する

```bash
git log --oneline
```

### 3. グラフ形式でブランチの流れを表示する

```bash
git log --oneline --graph
```

### 4. 特定ファイルの履歴を表示する

```bash
git log ファイル名
```

---

## 📋 出力例

```
* a1b2c3d (HEAD -> main) feat: section5 を更新
* d4e5f6a fix: section3 の誤字を修正
* 7g8h9i0 Initial commit
```

---

## 🔧 便利なオプション一覧

| オプション | 説明 |
|-----------|------|
| `--oneline` | 1 コミットを 1 行で表示 |
| `--graph` | ブランチのグラフを表示 |
| `--all` | すべてのブランチを表示 |
| `-n 5` | 最新 5 件だけ表示 |
| `--author="名前"` | 特定の作者でフィルタ |
| `--since="2 weeks ago"` | 期間でフィルタ |

---

> 💡 **ヒント:** `q` キーでログ表示を終了できます。  
> `--oneline --graph --all` を組み合わせると、全ブランチの流れが一目で分かります。

---

[← 前のセクション: 状態の確認](section5.md) ｜ [← マニュアル トップへ](gitmanual.md) ｜ [次のセクションへ → 差分の確認](section7.md)
