# 8. ブランチ操作

ブランチを使って、作業を分岐・切り替えします。

## 手順

1. ブランチの一覧を表示する
   ```
   git branch
   ```
2. 新しいブランチを作成する
   ```
   git branch ブランチ名
   ```
3. ブランチを切り替える
   ```
   git switch ブランチ名
   ```
4. ブランチの作成と切り替えを同時に行う
   ```
   git switch -c ブランチ名
   ```
5. ブランチを削除する
   ```
   git branch -d ブランチ名
   ```

> ブランチを活用すると、機能追加やバグ修正を独立して進められます。
